const http=require('node:http'),fs=require('node:fs'),path=require('node:path');
const root=__dirname,port=4173;
const gptApi=require('./gpt-api.cjs')();
http.createServer(async(req,res)=>{try{if(await gptApi(req,res))return;let url=decodeURIComponent(new URL(req.url,'http://localhost').pathname);let file=path.resolve(root,'.'+url);if(file!==root&&!file.startsWith(root+path.sep)){res.writeHead(403);return res.end()};if(fs.existsSync(file)&&fs.statSync(file).isDirectory())file=path.join(file,'index.html');const ext=path.extname(file);const type={'.html':'text/html; charset=utf-8','.css':'text/css; charset=utf-8','.js':'application/javascript; charset=utf-8','.json':'application/json; charset=utf-8','.jpg':'image/jpeg','.png':'image/png','.pdf':'application/pdf'}[ext]||'application/octet-stream';if(!fs.existsSync(file)){res.writeHead(404);return res.end('Not found')}res.writeHead(200,{'Content-Type':type,'Cache-Control':'no-cache'});fs.createReadStream(file).pipe(res)}catch{res.writeHead(400);res.end('Bad request')}}).listen(port,'127.0.0.1',()=>console.log('Local: http://127.0.0.1:'+port+'/'));
